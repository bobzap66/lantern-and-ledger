name: Deploy Quartz site to GitHub Pages

on:
  repository_dispatch:
    types:
      - vault-updated
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Quartz
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Checkout Obsidian vault
        uses: actions/checkout@v7
        with:
          repository: bobzap66/inner-sea-region-vault
          path: content
          fetch-depth: 0
          token: ${{ secrets.VAULT_TOKEN }}

      - name: Normalize vault note links
        run: node scripts/normalize-vault-note-links.mjs

      - name: Check vault link conventions
        run: node scripts/check-link-conventions.mjs

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Quartz plugins
        run: npx quartz plugin install --from-config --clean

      - name: Pin Explorer subdirectory navigation fix
        run: |
          npm install --no-save github:quartz-community/explorer#07de53f210f86287f41f0b349271c036bdc0b7c4
          npm install --no-save @quartz-community/utils@0.1.1

      - name: Normalize content asset paths
        run: node scripts/normalize-content-asset-paths.mjs

      - name: Expand folder galleries
        run: node scripts/expand-folder-galleries.mjs

      - name: Generate homepage recent notes
        run: node scripts/generate-homepage-recents.mjs

      - name: Build Quartz
        run: npx quartz build

      - name: Generate Golarion calendar data
        run: node scripts/generate-golarion-calendar.mjs

      - name: Merge campaign timeline metadata
        run: node scripts/merge-timeline-metadata.mjs

      - name: Diagnose Golarion calendar data
        run: node scripts/diagnose-golarion-calendar.mjs | tee golarion-calendar-diagnostic.txt

      - name: Upload calendar diagnostic
        uses: actions/upload-artifact@v4
        with:
          name: golarion-calendar-diagnostic
          path: golarion-calendar-diagnostic.txt
          retention-days: 1

      - name: Canonicalize rendered note links
        run: node scripts/canonicalize-built-note-links.mjs

      - name: Check internal links
        run: node scripts/check-built-links.mjs

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: public

  deploy:
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
